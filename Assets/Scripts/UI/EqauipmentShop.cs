using System.Collections;
using System.Collections.Generic;
using TPSGame.UI;
using UnityEngine;

public class EqauipmentShop : UIBase
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
